{
  "name": "@haikuweather",
  "scripts": {
    "setup": "npm i --workspaces",
    "rebuild": "npm run compile --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "@types/estree": "1.0.2"
  }
}
